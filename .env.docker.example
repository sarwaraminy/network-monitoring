# Copy to .env in the repository root, then `docker compose up --build`.
# This file configures the containers only; api/.env is for running on the host.

# --- Required ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET=
POSTGRES_PASSWORD=

# --- Optional ---
POSTGRES_DB=netminitoring
POSTGRES_USER=postgres

# Port the UI (and the proxied API) is published on.
HTTP_PORT=8080

# The name this sensor's findings are stored under. Only matters when a second
# sensor points at this same database: give each one its own name and their alerts
# and device lists stay apart. With both on `default` they merge into single rows,
# and whichever sensor learned a device first silences new-device detection on the
# other. Leave it alone if this is the only sensor.
SENSOR_ID=default

# trace | debug | info | warn | error | fatal
LOG_LEVEL=info

JWT_EXPIRES_IN=1d

# Public registration. Leave false; self-registered accounts are always USER.
ALLOW_OPEN_SIGNUP=false

# Threat intelligence. See api/.env.example for feed examples.
INTEL_ENABLED=false
INTEL_FEEDS=
INTEL_REFRESH_HOURS=6
INTEL_MAX_INDICATORS=500000
# The cache path inside the container is fixed by docker-compose.yml and backed
# by the `intel-cache` volume, so it is not set here.
CORS_ORIGIN=http://localhost:8080

# Blanks packet payloads in API responses. Consider enabling where captured
# traffic may contain personal data.
REDACT_PACKET_PAYLOAD=true

# Resume a capture interrupted by a restart. Off by default — a container that
# restarts on its own would otherwise begin capturing with nobody present.
CAPTURE_RESUME_ON_START=false
CAPTURE_BUFFER_SIZE=5000
ARP_TRUSTED_MAPPINGS=
VITE_POLL_INTERVAL_MS=1000

# --- Alert notifications ---
#
# Every setting in this section and in "Export: syslog / CEF to a SIEM" below is
# also editable by an administrator on the Delivery page, and a value set in THIS
# FILE pins it there: the control renders disabled and names the variable.
#
# They are therefore left BLANK, with each default recorded in a comment above
# it. Filling one in is how to keep that decision in the file, which is the
# guarantee a config-managed deployment wants — but having all sixteen filled in
# by default was how the Delivery page came to report "16 settings are set in the
# environment and cannot be changed here" on every install, with nothing an
# administrator could do about it.
# Without this, a critical finding lands in a table and nobody is told.
# default: false
NOTIFY_ENABLED=
# critical | high | medium | low | info. Leave at high.
# default: high
NOTIFY_MIN_SEVERITY=
# Slack, Teams, Discord, or anything accepting JSON. Treat as a secret.
NOTIFY_WEBHOOK_URL=
# default: auto
NOTIFY_WEBHOOK_FORMAT=
# Set false to keep internal IPs, MACs and usernames out of third-party chat.
# default: true
NOTIFY_INCLUDE_EVIDENCE=
# default: 60
NOTIFY_DIGEST_SECONDS=
# default: 900
NOTIFY_THROTTLE_SECONDS=
# default: 12
NOTIFY_MAX_PER_HOUR=
# Email, if you have an SMTP relay. A relay needs no credentials: leave SMTP_USER
# and SMTP_PASSWORD empty and no AUTH is sent at all.
NOTIFY_EMAIL_TO=
NOTIFY_EMAIL_FROM=
SMTP_HOST=
# default: 587
SMTP_PORT=
# default: false
SMTP_SECURE=
SMTP_USER=
SMTP_PASSWORD=
# password | oauth2. Microsoft 365 and Google disable basic SMTP AUTH by default, so
# a company mailbox usually needs oauth2 — the five below, and api/.env.example has
# the provider setups. Blank on purpose: password is the default anyway, and pinning
# it to `password` hides the OAuth2 fields on the Delivery page along with the
# disabled select. Pinning `oauth2` only disables the select and is supported.
SMTP_AUTH_METHOD=
SMTP_OAUTH_CLIENT_ID=
SMTP_OAUTH_CLIENT_SECRET=
SMTP_OAUTH_REFRESH_TOKEN=
SMTP_OAUTH_TOKEN_URL=
SMTP_OAUTH_SCOPE=

# --- Flow collection ---
# Only used with the flow override:
#   docker compose -f docker-compose.yml -f docker-compose.flow.yml up --build
# 2055 is the de facto NetFlow port; 4739 is IANA's for IPFIX.
FLOW_PORT=2055
# Comma-separated exporter addresses. Empty accepts any source, which is needed
# to discover them on a first run; set it once the devices are known.
FLOW_EXPORTERS=

# --- Export: syslog / CEF to a SIEM ---
# Set the host to switch it on. Not governed by NOTIFY_ENABLED or the gates
# above — a SIEM needs the complete stream, not a digest.
SYSLOG_HOST=
# default: 514
SYSLOG_PORT=
# default: udp
SYSLOG_PROTOCOL=
# default: cef
SYSLOG_FORMAT=
# default: 5424
SYSLOG_RFC=
# default: 16
SYSLOG_FACILITY=
# default: nmt
SYSLOG_APP_NAME=
# default: true
SYSLOG_INCLUDE_EVIDENCE=

# --- Behind a proxy ---
# On here, because the api is expose-only and every request arrives through the
# nginx ui service, which sets X-Forwarded-For. Off, the rate limiter keys every
# client to the nginx container and they share one bucket.
TRUST_PROXY=true

# Linked from notification messages.
NOTIFY_DASHBOARD_URL=

# --- Flow collection ---
# The driver-free path: point a switch or router at FLOW_PORT and no capture
# library is needed. Publishing the UDP port is a separate opt-in — see
# docker-compose.flow.yml.
FLOW_ENABLED=false
FLOW_BIND_ADDRESS=0.0.0.0

# --- Capture tuning ---
CAPTURE_POLL_INTERVAL_MS=10

# --- Rate limits ---
# The right values depend on how many people share an egress address.
RATE_LIMIT_AUTH_PER_MINUTE=20
RATE_LIMIT_CAPTURE_PER_MINUTE=20
RATE_LIMIT_LOOKUP_PER_MINUTE=30
RATE_LIMIT_API_PER_MINUTE=600

# --- Detector thresholds ---
# The tuning knobs. An authorised vulnerability scanner on the network is the
# usual reason to raise one — it will otherwise trip the port-scan and host-sweep
# detectors on every scan cycle.
DETECT_SCAN_WINDOW_MS=60000
DETECT_PORT_SCAN_PORTS=15
DETECT_HOST_SWEEP_HOSTS=20
DETECT_FLOOD_WINDOW_MS=10000
DETECT_SYN_FLOOD_ATTEMPTS=300
DETECT_DNS_SUBDOMAINS=40
DETECT_DEVICE_LEARNING_MS=60000
DETECT_ALERT_WINDOW_MS=300000

# --- Retention ---
# Alerts expire in detail after ALERT_RETENTION_DAYS and survive as daily rollups,
# which are never pruned, so a long dashboard window keeps its shape. Set
# RETENTION_ENABLED=false to keep everything. Under 7 days is clamped up with a
# warning: a slipped digit deletes findings irreversibly.
RETENTION_ENABLED=true
ALERT_RETENTION_DAYS=365
# Days of absence, not age: `last_seen` is refreshed while a device keeps appearing.
# Forgetting a device means it is reported as new if it comes back.
DEVICE_RETENTION_DAYS=365

# --- Ad Hoc Query console ---
# A read-only SQL page for administrators, for the question no screen answers.
# OFF by default: switched on without thought this is a SQL prompt on the
# production database, reachable from a browser session.
#
# Enforcement is a Postgres role (nm_adhoc, created by migration V10) that can
# only SELECT and cannot read the columns holding secrets — the SMTP password,
# the webhook URL, the password hashes. The role name is fixed in code and only
# its password is set here, so there is no configuration that reaches the
# superuser. The server refuses to enable the console unless the database
# confirms that role is neither a superuser nor able to write.
#
# Every setting below can also be changed by an administrator in the interface,
# under the settings gear — the password included, since V15. The environment
# wins: a variable set here PINS its field, and the interface shows the control
# disabled and names the variable rather than accepting an edit that would do
# nothing. So these are left blank — the code defaults apply, and ADHOC_ENABLED
# still defaults to false.
# Left BLANK rather than commented out: blank means "not decided here", so the
# code default applies (ADHOC_ENABLED is still false) and the field stays
# editable. Fill one in to take that decision back from the interface for good.
ADHOC_ENABLED=
# Required when enabled. Installed on the Postgres role at boot by a statement
# with no parameterised form, so the database's own log may capture it.
#
# Settable in the interface too, and setting it here pins it there — which is how
# to keep the "who may create a SQL prompt on production" decision with whoever
# has access to this file. Never returned by the API, and never written to the
# audit trail. Any strong value, not reused anywhere.
# NOTE: removing this line later does NOT switch the console off. Whatever it
# said was copied into the stored settings at first boot, so the row keeps the
# copy. Set ADHOC_ENABLED=false, or switch it off in the interface.
ADHOC_DB_PASSWORD=
ADHOC_TIMEOUT_MS=
ADHOC_MAX_ROWS=
# Lets the console UPDATE, INSERT and DELETE as well as read. OFF by default,
# and separate from ADHOC_ENABLED on purpose: switching the console on and
# letting it write are two decisions, and only one can destroy data from a
# browser session.
#
# It selects a different Postgres ROLE rather than merely permitting writes in
# the app, so a read-only install is read-only in the database rather than in an
# `if`. Write mode still cannot touch the audit trail, the accounts, the delivery
# settings or the columns holding secrets, and is still not a superuser: it
# writes the operational tables only. See V12__Adhoc_write_role.sql.
#
# Auditing is forced to `all` while this is on.
ADHOC_WRITE_ENABLED=
ADHOC_MAX_QUERY_LENGTH=
# How much of the console's activity reaches the audit trail:
#   all     - every query as it is accepted (the default; "who asked what" is
#             what an audit trail is for, and this is a SQL console over
#             security findings)
#   refused - only queries the database rejected, which is the set worth keeping
#             if you are reading the trail for attempts rather than activity
#   off     - nothing
ADHOC_AUDIT=
# Gap between sweeps, 1-596 hours (past that a JavaScript timer inverts into 1 ms).
RETENTION_SWEEP_HOURS=24
