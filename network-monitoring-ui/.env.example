# Leave empty to use the Vite dev proxy (recommended for local development).
# Set to the API origin for a production build, e.g. https://nmt.example.com
VITE_API_SERVER=

# How often the capture pages refresh the packet list, in milliseconds.
VITE_POLL_INTERVAL_MS=1000
